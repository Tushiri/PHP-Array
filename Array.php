<?php

// Create an array with the given fruit names
$fruits = array("apple", "banana", "orange", "pear");

// Loop through the array and output each element with its index
foreach ($fruits as $index => $fruit) {
    echo "array name at index[$index] is : $fruit <br>";
}

?>
